# MCP Remote SSH Server Configuration
#
# This file configures the connection to your remote GPU machine.
# Copy this to .env and fill in your values.

# SSH Configuration
# -----------------
# The MCP server needs the actual hostname/port, not SSH config aliases.
#
# For SkyPilot clusters:
# Run: ssh -G <your-cluster-name> | grep -E "^(hostname|port|user)"
# Example output:
#   hostname localhost
#   port 10022
#   user root
#
# Then set:
SSH_HOST=root@localhost
SSH_PORT=10022

# Optional: Path to SSH private key (if not using default)
# SSH_KEY_PATH=~/.ssh/sky-key

# Getting Your Configuration
# --------------------------
# 1. Find your SkyPilot cluster name:
#    sky status
#
# 2. Get connection details:
#    ssh -G <cluster-name> | grep -E "^(hostname|port|user)"
#
# 3. Update SSH_HOST and SSH_PORT above
#
# 4. Test connection manually:
#    ssh -p <port> <user>@<hostname> "echo 'Connection works!'"
#
# 5. Copy this file to .env:
#    cp .env.example .env
