# Scoring — anvil_pub.bb-workshop.10 (anvil-pub-v2, /44)

Reviewed against `.anvil/skills/pub/rubric.md` (9 dimensions, /44,
advance >= 35). Quoted spans are verbatim from `main.tex` per the
issue #464/#475 evidence discipline and were validated with
`anvil.lib.evidence_check` before this sidecar landed. Every headline
statistic was independently recomputed from the committed artifacts at
repository HEAD before scoring (see `findings.md` for the verification
log).

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 5 | Method discipline remains exemplary: the mean-field reduction with an owned 3–10x threshold gap, the pre-registered k* class test with exact/permutation p-values and a stated power floor, and the framing "we treat the analytical NE characterization as ground truth and the PPO trainability sweep as a falsification test, in that order" (— §1) are all sound, and v10's recompute-at-HEAD plus the honest withdrawal of the metric-robustness sentence strengthen the calibration. Held at 5/6 for the same structural reason as v9: the central §4 correlation rests on a 50-iteration budget whose class separations (0.045/0.036 on gap_closed_ne, verified against the HEAD artifact) the paper itself concedes are ordering-only, so the headline trainability claim remains under-powered relative to its prominence. |
| 2 | Evidence sufficiency | 6 | 5 | Evidence volume is strong (148-run sweep, 20-seed buy-down, 4x/16x ladders, 10k-episode paired batteries, 12/12 equilibrium coverage) and this review recomputed every headline number from the artifacts at HEAD — class means 0.142/0.096/0.060/-0.024, the homogeneous inversion 0.041 vs 0.046, rho=0.109 (p=0.56) with rho=0.007 verified at pinned revision 22b1fda6, the k* test battery, the -0.108 4x-budget mean, and the full rest_trap anchor ladder all match. Held at 5/6 because the §4 headline is still evidence-limited by design: "the data is consistent with the predicted ordering but does not reject the null of equal class means under a standard test" (— §4), and the mixed class's rank is conceded to be metric- and noise-sensitive, so the ordering claim rests on direction alone pending the named camera-ready significance gate. |
| 3 | Clarity of contribution | 5 | 5 | The abstract is now ~325 rendered words (down from ~700) and each contribution is extractable in one sentence from it; the §1 Contributions paragraph enumerates (1)–(5) crisply including the double negative result: "we retire two intuitive predictors in sequence and identify the honest remaining signal" (— §1). The v9 deduction (one-page abstract defeating extraction) is fully resolved; a busy PC member can extract the contribution set from the abstract alone in 90 seconds. |
| 4 | Related-work positioning | 5 | 4 | The v9 gap is substantively addressed: the new §6 paragraph engages OpenSpiel and PSRO/NashConv and re-scopes the novelty claim honestly — "it is not the first environment where equilibrium convergence is measurable (that credit belongs to the exploitability tradition)" (— §6) — and the abstract claim now carries the "parametric ... across a controlled grid" qualifier. Held at 4/5 because the engagement is thin (two citations entered from memory, flagged in refs.bib as unverified on disk) and the exploitability-evaluation line's empirical follow-ups and any other parametric-family game constructions are not surveyed; no litsearch sibling exists, so this scores on the legacy baseline (no deduction for perspective absence) and a `pub-litsearch` re-run remains the right verification step. |
| 5 | Reproducibility | 5 | 4 | The apparatus is unusually strong and the v9 staleness defect is verifiably fixed: this review re-derived the §4 class means from `recalibrated_verdict.json` at HEAD and re-rendered Figure 2 from the preserved `figures/src/recalibrated_heatmap.py` (identical text layer to the shipped PDF), so the claim that the v2 artifacts "reproduce byte-for-byte from the committed per-seed summaries" (— §7 Reproducibility) is now literally true. Held at 4/5 for two residual gaps the paper itself names: "the publication pathway for the baselines is in progress and not yet complete at the time of this draft" (— §7), and the 300/300 bootstrap-combination sweep traces to a PR-review recomputation rather than a regenerable `experiments/` artifact (footnoted as below the paper's usual standard). |
| 6 | Figure & table quality | 4 | 3 | The v9 defects are fixed (unsampled-cell descriptor corrected at all three sites and verified against `results.json`; Table 2's provenance column no longer overflows; both figures are self-contained and print-safe), but the caption sentence added to fix the v6 carry-forward introduces a new factual error: Figure 1's caption says "within each panel rows are $\kappa$ and columns are $\beta$" (— Fig. 1 caption) while the rendered figure (and `figures/src/phase_diagram.py`, which builds a 3-beta-row by 5-kappa-column grid) has rows = beta and columns = kappa — the caption contradicts the figure's own axis labels and its own later "$\kappa\in\{0.3,0.7\}$ columns" clause. One transposed descriptive clause in the headline figure's caption is exactly the caption/figure-contradiction defect class CF-2 targeted, so this cannot score full weight. |
| 7 | Prose & structural quality | 4 | 4 | Structure and flow are intact, tense and voice consistent, and this review's independent `pdflatex + bibtex + pdflatex x2` build of the v10 source completes at 27 pages with no instance of an overfull hbox found in the build log (v9 had 10, five severe at 96.7–128.9pt) and no instance of a broken cross-reference or unresolved citation found; the xurl/`\path{}` treatment, display-math reward tuple, and itemized verdict ladder resolve every flagged site. Residual log noise (duplicate hyperref destinations from `\tag`-ed display equations, `h`-float promotions) is cosmetic. |
| 8 | Citation hygiene | 5 | 5 | All 11 cite keys resolve against refs.bib with complete author/title/venue/year fields plus arXiv IDs, there are no orphan entries, and the final compile pass shows no instance of an undefined citation found; the two new entries (openspiel2019, psro2017) are honestly flagged in the .bib header for litsearch/audit verification, matching the ppo2017/mappo2022 precedent, and repo-internal results cite committed artifact paths per the paper's convention. The Appendix A named-template attributions remain uncited by explicit, documented decision (no verifiable source on disk) — a litsearch work item, not a hygiene defect. |
| 9 | Rhetorical economy | 4 | 3 | The largest v9 duplication is gone — the abstract dropped from ~700 to ~325 words with per-test statistics moved to their owning sections — and the caveat restatements are collapsed. Held at 3/4 because the unnumbered conclusion still restates the full result arc at near-abstract length beginning "We presented Bucket Brigade, a parametric cooperative-competitive MARL benchmark whose Nash-equilibrium structure is characterisable in closed form" (— Conclusion), duplicating the abstract's opening move, and the 27-page artifact (13 body pages before appendices) still serves a 4-page workshop brief; the appendix policy is an operator decision, but the body itself could land the same argument in fewer pages. |
| | **Total** | **44** | **38** | |

Perspective substrate note (dim 4): no `pub-litsearch` sibling exists in
this portfolio, so dim 4 is scored on the legacy baseline (no deduction
for perspective absence per the opportunistic-not-punitive rule); the
score reflects the residual thinness of the new engagement, not the
missing sibling.
