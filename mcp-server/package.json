{
  "name": "@bucket-brigade/mcp-remote-ssh",
  "version": "0.1.0",
  "description": "MCP server for remote SSH command execution",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-remote-ssh": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsx src/index.ts",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "ssh",
    "remote",
    "claude"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "ssh2": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ssh2": "^1.15.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
