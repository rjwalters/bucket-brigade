{
  "name": "bucket-brigade-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "install:all": "uv pip install -e .[dev] && cd web && pnpm install",
    "format": "black . && cd web && pnpm run format",
    "lint": "ruff check . && cd web && pnpm run lint:biome",
    "lint:fix": "ruff check . --fix && cd web && pnpm run lint:biome:fix",
    "typecheck": "mypy . && cd web && pnpm run typecheck",
    "test": "pytest && cd web && pnpm run test",
    "test:python": "pytest",
    "test:web": "cd web && pnpm run test",
    "test:web:headed": "cd web && pnpm run test:headed",
    "clean": "find . -type f -name '*.pyc' -delete && find . -type d -name __pycache__ -delete && rm -rf .pytest_cache",
    "dev": "cd web && pnpm run dev",
    "build": "cd web && pnpm run build"
  },
  "devDependencies": {
  }
}
