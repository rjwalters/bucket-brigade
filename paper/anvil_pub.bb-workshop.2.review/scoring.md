# Per-dimension scoring — anvil_pub.bb-workshop.2

Rubric: `.anvil/skills/pub/rubric.md` (9 dimensions, /44, advance ≥35).

| # | Dimension | Weight | Score | Justification |
|---|---|---:|---:|---|
| 1 | Rigor of method / argument | 6 | **5** | §3 derives closed-form boundary inequalities (eqs. S/A/C) via an explicit single-house mean-field reduction with $\tilde A{=}A\rho{=}36.24$ as the survival coefficient. The 3–10× quantitative gap against the equilibrium solver is owned in the same section, with three named systematic biases (ring locality, per-agent ownership, heuristic-strategy space). §4's PPO protocol pins hyperparameters in-repo, uses the in-repo `JointPPOTrainer`, and reports the per-cell `gap_closed_ne` metric with a methodology paragraph naming `bucket_brigade/baselines/per_cell.py`. The one rigor weakness is the β-independence claim at the PPO layer: it is grounded in a 0.017 cell-to-cell difference at κ=0.5 against a seed std of ~0.33, which the paper acknowledges as "well within seed variance" — so the claim reads as "the PPO data is consistent with β-independence", not "the PPO data demonstrates β-independence." That is a small softening, well-handled but worth noting. |
| 2 | Evidence sufficiency | 6 | **4** | The headline ordering (symmetric 0.326 > asymmetric 0.106 > collapse −0.033) is supported on 7 cells × 4 seeds = 28 runs, with n=2/2/3 cells per NE class. The §4 Caveats paragraph names this explicitly. The within-class statistics are very underpowered for an ordering claim: the symmetric-class std is ~0.33 and the within-class spread (0.334 vs 0.317) is 0.017, an order of magnitude smaller. The paper survives by claiming the *ordering*, not within-class effects, and by reporting the same qualitative ordering under the `gap_closed_homogeneous` fallback metric. But "n=2 cells per NE class with std comparable to the means" is the weakest part of the empirical contribution and is the natural target for a reviewer challenge. A program-committee member familiar with MARL trainability sweeps would expect n=5+ cells per class even at workshop scope. The deferred 75-cell full sweep, named as future work, is honest disclosure but does not patch the present evidence. |
| 3 | Clarity of contribution | 5 | **5** | Abstract states the contribution unambiguously: a parametric game with computable NE structure, three NE regimes, the ordering result, and the methodological per-cell baseline finding. §1's Contributions paragraph lists 4 items, each one-sentence-extractable. The "methodological complement, not replacement" framing is repeated consistently across abstract, §1, §5, §6, and Conclusion. The contribution that the v2 newly elevates — that per-cell baselines are required for cross-cell PPO comparison whenever the random-policy return varies with the cell — is named as "a separate contribution" in §4 and named again in the Conclusion. |
| 4 | Related-work positioning | 5 | **4** | §5 + Table 1 cover the six canonical MARL benchmarks (Overcooked, Melting Pot, Hanabi, SMAC/SMACv2, MAgent, PettingZoo MPE) with year, agent count, per-agent action space, NE characterizability, and coop/competitive axis. The honest positioning is hammered: "loses on every other axis", "methodological complement, not a replacement". The forbidden phrases ("emergent cooperation", "general-purpose MARL benchmark", "scales to large populations") appear only in explicit negation context. The one gap is that no `pub-litsearch` sibling was run, so dim 4 does not get the substrate-backed bonus that the rubric's "Perspective substrate" subsection describes. The legacy baseline applies: the paper scores against the related-work prose alone, which is well-executed but not validated against a candidates.bib. No close prior work is obviously omitted in the reviewer's judgment; the six benchmarks chosen are the right reference set for the workshop's audience. |
| 5 | Reproducibility | 5 | **4** | §6 Reproducibility paragraph names: `pip install bucket-brigade` for the env; `docs/PAPER_RESULTS.md` for figure-by-figure recipes; `bucket_brigade/baselines/per_cell.py` and `experiments/nash/phase_diagram/per_cell_baselines.json` for the per-cell baselines; GitHub URL for the source. HuggingFace baselines are explicitly named as "in progress, not yet complete at the time of this draft" — honest and trackable. §4 Protocol pins "$50$ training iterations of $2048$ rollout steps and $4$ random seeds per cell" and names `JointPPOTrainer` and `experiments/p3_specialization/` for hyperparameters. Compute disclosure ("single 16-core host in approximately two hours") is vague by modern reproducibility standards (no CPU model, no memory footprint, no wall-clock breakdown) but is workshop-acceptable. Specific seed values are not enumerated in the paper itself, which is a minor gap; the in-repo seed configuration is the load-bearing reproducibility artifact. |
| 6 | Figure & table quality | 4 | **3** | Figure 1 (phase_diagram.png) caption is self-contained: names the parameter cell ($c{=}0.5,\rho{=}0.02,T_{\min}{=}12$), the heuristic-Double-Oracle solver, the three regimes, the β-independence observation, and the κ-threshold disagreement against §3. Table 1 (benchmark comparison) is clean, with consistent alignment and a "Year / Agents / Per-agent |A| / NE characterizable? / Coop/comp" column set. Figure 2 (recalibrated_heatmap.tex) renders the per-cell values as text in colored boxes with hatching to flag the κ=0.1 metric fallback to `gap_closed_homogeneous` and a dagger marker. The caption explains the hatching and the β=0.1 column absence. Two concerns lower the score: (a) Figure 2 reports std values (±0.331, ±0.344) as text labels rather than visual error indicators, which makes it hard to read off the relative-significance signal at a glance; (b) the hand-built TikZ grid layout is fragile compared to a matplotlib render (which the figure's header comment acknowledges as a planned replacement). Neither defect breaks the figure's content but a print-quality render would score higher. |
| 7 | Prose & structural quality | 4 | **3** | LaTeX compiles cleanly in source (no `Section ??`-style broken refs, no unused macros, every `\label{}` is referenced). Section flow is the canonical Introduction → Environment → NE structure → Trainability → Related work → Discussion/Limitations → Conclusion. Tense and voice are consistent (technical present + first-person plural where appropriate). The one structural concern is page-fit risk: at 3705 words (per `wc -w`), this is at the high end of the 3000–4000 word target named in the BRIEF for a 4-page workshop format. The 7-section + 2-figure + 1-table layout will be tight in 4 pages with `anvil-paper.cls`. This is a render-gate concern, deferred to `pub-audit`, but the source-side word count suggests the paper is over-spent by 200–500 words for the venue. |
| 8 | Citation hygiene | 5 | **4** | All 11 `\cite{}` keys (`overcooked2019`, `meltingpot2021`, `hanabi2020`, `smac2019`, `smacv2_2023`, `magent2018`, `pettingzoo2021`, `bbenvspec`, `bbnestructure`, `ppo2017`, `mappo2022`) resolve to entries in `refs.bib`. Every bibliography entry has the full author / title / venue / year / arXiv ID set. The two internal-memo cite keys (`bbenvspec`, `bbnestructure`) are `@misc` entries with a `note` field flagging the arXiv-preprint replacement before submission — already triaged in the bib comments. No malformed entries; no obvious uncited claims in §1–§6 main bodies. The pub-review stage does not verify cited papers actually support the claims (that's `pub-audit`'s job), so this score reflects only the structural side. Score is 4/5 rather than 5/5 because the two `@misc` internal-memo cites are not yet arXiv-resolvable — a substitutable defect, but a real one at submission time. |
| 9 | Rhetorical economy | 4 | **3** | The paper is mostly tight. The abstract states the contribution and the two numerical headlines (0.326 and 0.106) in ~270 words. §3's Headline finding paragraph is load-bearing and well-placed. §4's methodological-observation paragraph is a contribution worth keeping prominent. The minor cost-additions: (a) §6 (a)/(b)/(c) defense and the analytical-threats paragraph carry some overlap on the "size is the cost of NE transparency" theme — could be a single tighter paragraph; (b) the related-work prose at the end of §5 ("we do not claim... we do not claim... we do not claim... It is none of these things") repeats the disclaim three times in slightly different phrasings; (c) the closing sentence of §3's Predicted-vs-observed paragraph re-states the "ring-locality + per-agent ownership + heuristic-strategy" trio after the bulleted (i)/(ii)/(iii) enumeration has already named them. At 3705 words against a 4-page target, these are real costs against the budget. None are dishonest; all are tightenable. |
| | **Total** | **44** | **35** | |

## Critical flags

None set by the reviewer. The render-gate (mechanical pre-flight) is
deferred because `paper.pdf` and `compile-log.txt` are absent — `pub-audit`
has not yet run.

## Calibration notes

- The /44 advance threshold of 35 is met exactly. This is intentionally
  marginal scoring: the v2 paper is solid and the qualitative ordering
  result is real, but the n=2-per-class sample size and the std-vs-mean
  ratio on Figure 2 are real evidence-side weaknesses a determined
  reviewer would call out. The paper advances, but the reviewer would
  prefer to see the 75-cell sweep land for the camera-ready.
- The NeurIPS overlay (13/16) is informational only and does not change
  the gate.
- No dimension scored below ~50% of its weight; the lowest relative
  score is dim 9 (Rhetorical economy) at 3/4, driven by the word-count
  margin and a few tightenable paragraphs.
