# Per-dimension scoring — anvil_pub.bb-workshop.5

Rubric: `.anvil/skills/pub/rubric.md` (9 dimensions, /44, advance ≥35).

| # | Dimension | Weight | Score | Justification |
|---|---|---:|---:|---|
| 1 | Rigor of method / argument | 6 | **5** | §3 derives closed-form boundary inequalities (S/A/C) via an explicit single-house mean-field reduction with $\tilde A=A\rho=36.24$ as the survival coefficient. The 3–10× quantitative gap against the equilibrium solver is owned in the same section with three named systematic biases (ring locality, per-agent ownership, heuristic-strategy approximation), and the new Appendix B §B.5 expands the bias accounting to five sources (adding single-stage-vs-multi-stage SPE and β-independence-as-leading-order). The §4 ordering-not-significance disclosure (L471–476) is a real rigor improvement on v2: per-class std (0.21–0.44) is explicitly noted as larger than class-mean separations (0.073, 0.048, 0.108), and the named "cross-class 4×-budget sweep is the natural significance gate" is honest about what would be required to upgrade the claim from ordering to test. One rigor weakness remaining: the §B.2 derivation of the survival coefficient $A$ uses a small-$q(k)$ Taylor expansion ($(1-q(k))^{T_{\min}}\approx 1-T_{\min}q(k)$) that the appendix does not name as the source of one of the §B.5 systematic biases — a careful reader will notice this is a linearisation step distinct from the ring-locality and heuristic-strategy corrections. Not load-bearing for the qualitative claim. |
| 2 | Evidence sufficiency | 6 | **5** | The headline ordering rests on $n=11/9/11/6$ cells across the four NE classes on the 37-cell PPO sweep (v4-audited; carried unchanged into v5). v5 adds Table 1 (per-κ predicted-class share on the full 39-cell Nash grid: 0/9, 6/6, 6/9, 0/6, 7/9), which gives a reader an inline view of where the analytical prediction does and does not hit the modal empirical class without flipping to the appendix. The 4×-budget structural-failure disambiguation ($-0.049\to-0.108$ on no_convergence cells) is the right experiment for the right claim. The remaining evidence-side weakness is the same one v2 flagged and v4 audited: per-class std is comparable to class-mean separation, so the ordering claim is consistent-with rather than test-of the prediction. v5 owns this in prose (L471–476) and names the cross-class 4×-budget sweep as the camera-ready significance gate. That is the right amount of honesty for a workshop pitch. |
| 3 | Clarity of contribution | 5 | **5** | The §1 Contributions split (3a) / (3b) directly addresses the v2 reviewer's flag that the per-cell baseline correction was buried in a sub-clause. v5 now reads: (3a) protocol + 4-class results + 4×-budget falsification; (3b) per-cell NE-anchored metric as a standalone methodological contribution with the single-cell-baseline-inverts-the-ordering finding stated up front. Conclusion (L698–702) carries the same separation. Abstract (L43–78) gives a reader the four NE classes, the ordering, the 3–10× gap, and the methodological-complement framing in a single 270-word paragraph. The contribution is unambiguous and one-sentence-extractable per item. |
| 4 | Related-work positioning | 5 | **4** | §5 + Table 2 cover the six canonical MARL benchmarks (Overcooked, Melting Pot, Hanabi, SMAC/SMACv2, MAgent, PettingZoo MPE) with year, agent count, per-agent action space, NE characterisability, and coop/competitive axis. The honest positioning is consistent ("loses on every other axis", "methodological complement, not a replacement"). v5 does not regress dim 4 from v2. The score is held at 4/5 (not raised) for the same legacy-baseline reason: no `pub-litsearch` sibling was run, so the perspective-substrate bonus the rubric describes does not apply. No close prior work is obviously omitted; the six benchmarks are the right reference set for the workshop's audience. The Volunteer's Dilemma / Public Goods / Stag Hunt named-attribution in Appendix A §A.6 ships without `\cite{}` links — a citation hygiene minor flagged in `comments.md` but not enough to lower dim 4. |
| 5 | Reproducibility | 5 | **5** | The two new appendices materially upgrade dim 5. Appendix A is a self-contained mathematical contract: state cardinality formula, the seven-phase ordering with the load-bearing comment, the independent-workers extinguish formula, the per-agent reward decomposition with `\mathrm{owner}(h)=h\bmod N`, and Table 3 notation summary. Appendix A §A.7 explicitly says "a reader who reimplements from this appendix alone — using any RNG and any programming language — should obtain matching equilibrium structure and policy-evaluation results." This is the calibration the rubric asks for. Appendix B §B.1–§B.3 spells out the derivation of (S)/(A)/(C) line-by-line, including the small-q Taylor step and the c_gap convention. §6 Reproducibility paragraph names `pip install bucket-brigade`, `docs/PAPER_RESULTS.md`, `bucket_brigade/baselines/per_cell.py`, `experiments/nash/phase_diagram/per_cell_baselines.json`, and the GitHub URL. HuggingFace baselines pathway disclosed as "in progress" — honest but tracked. |
| 6 | Figure & table quality | 4 | **2** | This is the major regression vs. v2's 3/4. Two distinct rendering defects: (a) Figure 1 (`phase_diagram.png`, unchanged from v4) shows raw numerical payoff values inside heatmap cells (-9693, -648, 72) rather than the categorical regime label the caption describes. The legend lists four regimes (symmetric_only, mixed, asymmetric_only, no_convergence) but the `mixed` swatch is unused in the plotted c=0.5 panel, and two unsampled cells render as bare em-dashes. A reader who reaches the figure cannot read off the four-regime story the §3 prose is making. The image is also a static PNG with no source script in `figures/src/`, so a re-render with corrected encoding is not one-command. (b) Table 2 (benchmark comparison, p.7) overflows the right margin — the "Coop/comp" column header and entries ("coop", "mixed", "vs. scripted", "per-scenario", "mixed within cell") are clipped at the page edge. Tables 1 (per-κ counts), 3 (notation), 4 (predicted-NE summary), and 5 (7-cell preview) are clean. Figure 2 (`recalibrated_heatmap.pdf`) is the v4-audited well-rendered three-panel matplotlib figure with Tufte-style std bars and the gap_closed_homogeneous fallback explained in the caption — that figure is fine. The two defects cut dim 6 from a defensible 3/4 to a 2/4 because together they affect the two visual artefacts a workshop reviewer will skim first. |
| 7 | Prose & structural quality | 4 | **3** | LaTeX compiles cleanly (3 pdflatex passes + bibtex per v4 audit, carried into v5 without regression: no `Section ??` broken refs, no `[??]` unresolved cites, no overfull/underfull boxes in the body sections). Section flow is canonical (Abstract → Intro → Env → NE structure → Trainability → Related work → Discussion → Conclusion → References → Appendices A,B). One real prose-quality issue: §B.4 "Predicted vs. empirical phase table (7-cell preview)" is essentially empty — the subsection contains one lead-in sentence ("The 7-cell preview at c=0.5, ρ=0.02 against the predicted verdict:") and then immediately the §B.5 header. The 7-cell preview Table 5 floated to mid-page 17 inside §B.5. The §B.4 / §B.5 boundary reads as a layout accident, not a structural choice. Two body→appendix references promise content the appendix does not deliver: §3 L389 promises a "per-cell predicted-vs-empirical table" in Appendix B (but only the 7-cell preview Table 5 and the per-κ-band summary Table 4 exist); Appendix B §B.1 references §B.5 before §B.5 has been introduced (forward reference, common but worth a heads-up). Word count post-appendix-inlining is high but the user has opted out of workshop budget enforcement. |
| 8 | Citation hygiene | 5 | **5** | v5 fully resolves the v4 M1 placeholder-citation flag: `bbenvspec` and `bbnestructure` `@misc` entries deleted from `refs.bib` (the comment block explaining the placeholder convention is also removed). The 9 remaining `\cite{}` keys (overcooked2019, meltingpot2021, hanabi2020, smac2019, smacv2_2023, magent2018, pettingzoo2021, ppo2017, mappo2022) all resolve in the rendered PDF (References section, p.9). Every bibliography entry has the full author/title/venue/year/arXiv ID set. No malformed entries; no `[??]` in the PDF. Appendix A §A.6 names Volunteer's Dilemma, N-player Public Goods, Stag Hunt, and free-rider problem as informal attributions without `\cite{}` links — the changelog (line 53) flags this as intentional to match Appendix A's informal-attribution style, but for the camera-ready a workshop reviewer may expect a `\cite{}` per named template. This is a minor, not enough to deduct the full point; dim 8 remains at 5/5 because the structural side (every cite resolves; every bib entry well-formed) is clean. |
| 9 | Rhetorical economy | 4 | **3** | The §6 Threats merge (v4's two paragraphs → v5's single "Threats to the contributions" paragraph) is the right move and saves ~80 words. The §4 Caveats paragraph compression (sample-size disclosure no longer redundant with the new ordering-not-significance sentence) is real. The Figure 1/2 caption polishes are real. Net rhetorical economy of the body (§1–§6 + Conclusion) is high. The dim 9 weight against v5's 3/4 score is driven by the appendices: at 247 + 294 source-markdown lines ported as appendix prose, both appendices include sub-paragraphs that re-cover ground from the body. Appendix A §A.6 (Volunteer's Dilemma / Public Goods / Stag Hunt / free-rider / stochastic-game templates) re-states §2 L195–203 with elaboration, but the body version is already adequate for the contribution argument. Appendix B §B.5 expands the §3 three-bias list to five biases (adding β-independence-as-leading-order and single-stage-vs-multi-stage SPE) — the two added biases are genuinely new content but they re-frame the §3 discussion rather than introducing new evidence. A camera-ready pass that promoted the appendices to supplementary materials would let the body alone (8 pages through Conclusion before References, per the changelog) read tighter than the 20-page integrated document. |
| | **Total** | **44** | **37** | |

## Critical flags

None set by the reviewer.

## Calibration notes

- v5 nets +2 on the /44 score vs. v2 (35 → 37), driven by dim 2 +1
  (calibrated ordering-not-significance disclosure + the new inline
  Table 1), dim 5 +1 (the two new appendices materialise the
  reproducibility contract), and dim 8 +1 (placeholder cites
  resolved), partially offset by dim 6 −1 (Figure 1 categorical
  encoding and Table 2 overflow), dim 7 unchanged at 3/4 (LaTeX
  compile is clean but §B.4 is structurally empty), and dim 9
  unchanged at 3/4 (the appendix bulk re-covers some body ground).
- The dim 6 deduction is the leverageable revision priority: a
  one-evening fix to Figure 1 (re-encode as categorical regime
  heatmap, three c-panels) and Table 2 (column wrap or `table*`)
  would lift dim 6 to 3/4 and the total to 38/44.
- The NeurIPS overlay was not re-scored in this pass (the v2 review
  produced a 13/16 advisory; v5's appendix additions do not
  materially shift the overlay's `soundness`, `presentation`,
  `contribution`, `novelty`, `reproducibility` axes; the dim 6
  Figure 1 finding would be the one place a re-scored overlay's
  `presentation` axis would move down by 1).
- No dimension scored below ~50% of its weight. The lowest relative
  score is dim 6 (Figure & table quality) at 2/4 = 50%.
